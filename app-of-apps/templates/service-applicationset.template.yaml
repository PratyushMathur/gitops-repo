apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: <service-name>-by-cluster
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  gitops.argoproj.io/enabled: "true"
                  services.<service-name>: "true"
          - git:
              repoURL: https://github.com/PratyushMathur/gitops-repo.git
              revision: HEAD
              directories:
                - path: app-of-apps/charts/<service-name>
  template:
    metadata:
      name: '{{.name}}-{{.path.basename}}'
      labels:
        app.kubernetes.io/part-of: sample-root-app
        app.kubernetes.io/managed-by: applicationset
        app.kubernetes.io/name: <service-name>
    spec:
      project: default
      source:
        repoURL: https://github.com/PratyushMathur/gitops-repo.git
        targetRevision: HEAD
        path: '{{.path.path}}'
      destination:
        server: '{{.server}}'
        namespace: '{{.path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
